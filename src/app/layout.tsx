import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800", "900"],
});

export const metadata: Metadata = {
  title: "IPChat — Ephemeral Chat Rooms",
  description:
    "Zero-login, zero-trace, ephemeral chat rooms. Enter a shared room code and start chatting. Messages vanish when you leave.",
  keywords: ["chat", "ephemeral", "anonymous", "room", "messaging"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} antialiased`}>{children}</body>
    </html>
  );
}
